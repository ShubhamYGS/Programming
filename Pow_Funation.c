#include<stdio.h>
#include<math.h>
int main()
{
	int a;
	a=pow(2,3);       //pow() function is a standard library function. Used for showing the power.
					 //In this example it will calculate the 2^3
	printf("%d",a);
}
